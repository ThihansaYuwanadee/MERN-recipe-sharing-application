PORT = 5000
MONGODB_URI ='mongodb+srv://sasankadini:ipLvllHFvJnfS9e1@cluster0.apo2f.mongodb.net/?appName=Cluster0'

function connectDB() {
	mongoose.connect(MONGODB_URI, {
		useNewUrlParser: true,
		useUnifiedTopology: true,
	}).then(() => {
		console.log('Database connected.....')
	}).catch((err) => {
		console.log("can't connected to DB",err)
		process.exit(1)
	})
}

module.exports = { connectDB };



